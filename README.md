# WolFox Test Modules

Linux build project for the allowlisted WolFox laboratory packages `com.wolfox.testapp` and `com.wolfox.sandbox`.

The GitHub Actions workflow builds SAFE, CAMERA, and MULTI test modules and uploads verified APK artifacts.
